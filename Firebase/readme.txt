The two files I used when trying out authentication using firebase
They won't work on their own. 
-- To use off line, you need to install firebase, login and serve the page locally.
-- otherwise you need to create a firebase project to use it in.

I used, among other source, 
https://firebase.google.com/docs/auth/web/firebaseui
